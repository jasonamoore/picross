Todo

onKeyPress/Release events -
  implement by saving a timestamp, processing all key presses since that last timestamp
  one element at a time may be the "focused" element (or null if new state)
  the focused element receives key events. focus changes one click, or by using tab key.
  potential "onFocus/Unfocus" event - may want to visually signal that an element is focused
 
 better/cleaner handling of drawing on the blanket
 
 color palette
 
 artwork for the remaining buttons
 
 test visibility of plates on blanket